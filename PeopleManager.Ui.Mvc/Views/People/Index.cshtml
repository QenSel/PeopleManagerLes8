@model IList<Person>

@{
    ViewData["Title"] = "People Index";
}

<h1>People Index</h1>

<a asp-action="Create" class="btn btn-primary">Create new Person</a>

<table class="table table-striped">
    <thead>
    <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var person in Model)
    {
        <partial name="PersonRowPartial" model="person"/>
    }
    </tbody>
</table>

@section Modals
{
    <div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="deleteModalLabel">Delete Person</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to delete this person?</p>
                </div>
                <div class="modal-footer">
                    <form method="post" asp-action="DeleteConfirmed">
                        <input type="hidden" name="id"/>
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">No</button>
                        <button type="submit" class="btn btn-primary">Yes</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
}

@section Scripts
{
   @* <script>
        $(function(){
            $("button[data-role='delete']").on("click", 
                function()
                {
                    let id = $(this).data("id");
                    $("input[name='id']").val(id);
                });
        });
    </script>
*@
    <script>
        $(function () {
            // This is a jQuery shorthand for $(document).ready(). It executes the enclosed code when the document has finished loading.

            $("button[data-role='delete']").on("click",
                function () {
                    // Selects all buttons with the attribute data-role="delete" and attaches a click event handler to them.

                    let id = $(this).data("id");
                    // Retrieves the value of the "data-id" attribute of the clicked button using jQuery's data() method.
                    // $(this) refers to the clicked button element.

                    $("input[name='id']").val(id);
                    // Sets the value of the input field with the name attribute "id" to the value of the variable 'id'.
                    // $("input[name='id']") selects the input field by its name attribute using jQuery, and .val() sets its value.
                });
        });
    </script>
}
